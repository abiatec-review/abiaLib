import React from 'react';

import Button from 'components/Button';

import './index.scss';

const App: React.FC = () => (
  <div className="app">
    <Button className="app_button">
      HelloButton
    </Button>
  </div>
);

export default App;
